{
  "name": "ortez-cms-auth",
  "version": "1.0.0",
  "private": true,
  "description": "Sveltia CMS auth bridge: Google OAuth -> GitHub App installation token (Cloudflare Worker)",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test src/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.87.0"
  }
}
